# Auth + Cloud DB attempt (WIP, not merged)

## Goal
Attempted to move authentication/user access to a cloud database-backed approach.

## What I implemented / changed
- [Brief bullet list: classes/files touched, what you added]
- [e.g., AuthManager changes, DB init code, user roles storage, etc.]

## What didn’t work (symptoms)
- [e.g., DB init failing on Tsuru / credentials not loading / driver not found / network access issue]
- [Include any key error messages if you have them]

## What I tried to debug
- [e.g., checked env vars, logs, connection string, init order, etc.]

## Why we decided to drop it
Time constraints + instability; submitting the stable branch instead.

## Next steps (if someone continues)
- [e.g., verify JDBC driver available on Tsuru, confirm DB URL/SSL params, ensure env vars set, add health-check endpoint, etc.]
